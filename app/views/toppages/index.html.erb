<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Welcome to the Animera!</h1>
      <%= link_to 'Animeraを始める!', signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>