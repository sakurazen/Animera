<ul class="media-list">
  <% @anime.reviews.each do |review| %>
    <li class="media">
      <div class="media-body">
          <div>
            <h2><%= link_to "#{review.episode_no}話", review_path(review)%></h2>
          </div>
          <div>
            <% if current_user == review.user %>
              <%= link_to "Delete", review, method: :delete, data: { confirm: "You sure?" }, class: 'btn btn-danger btn-xs' %>
            <% end %>
          </div>
      </div>
    </li>
  <% end %>
  <%= paginate reviews %>
</ul>

